define(function(require, exports, module) {
	var app = require('filter/cashFilter'), Conf = require('common/config');
	
	module.exports = app.factory('cashService', ['$http', 'URLS', function($http, urls){
		
		
	}]);	
});